{
  "name": "herdr-sprites-plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "check": "node --check src/action.mjs && node --check src/core.mjs && node --check src/workspace.mjs && node --check src/bridge.mjs && node --check src/confirm.mjs && node --check scripts/live-test.mjs && npm test"
  }
}
